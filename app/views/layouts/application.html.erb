<!DOCTYPE html>
<html>
<head>
	<meta charset="UTF-8">
	<title>ChatApp</title>
	<%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
	<%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
	<%= csrf_meta_tags %>
	<link href='http://fonts.googleapis.com/css?family=Playfair+Display' rel='stylesheet' type='text/css'>

</head>

<body>

	<div id='navBar'>

		<div id='icon'>
			one2many 
			<% if current_user != nil %>
			<%= link_to(image_tag(current_user.avatar.url(:thumb)), user_path(current_user)) %> <%= link_to(current_user.username, user_path(current_user)) %>
			<% end %>
		</div>

		<div id='logSign'>
			<%= link_to 'Home', rooms_path %> /
			<% if current_user != nil %>
				<%= link_to "Logout", session_path(:current_session), method: :delete %>
			<% else %>
				<%= link_to "Login", new_session_path %> /
				<%= link_to "Signup", new_user_path %>
			<% end %>
		</div>
	</div>
	<br>

	<div class='main'>
		<%= yield %>
	</div>

</body>
</html>
